mutation CreateIngredient($ingredient: IngredientInput!) {
  createIngredient(ingredient: $ingredient) {
    id
    name
    available
    ingredientCategory {
      id
      name
    }
  }
}
