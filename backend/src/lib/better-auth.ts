// import { betterAuth } from "better-auth";
// import { betterAuth } from "better-auth";
// import { Pool } from "pg";

// export const backendAuth = betterAuth({
//   database: new Pool({
//     connectionString: process.env.DATABASE_URL,
//   }),
// });

// export const loadBackendAuth = async () => {
//   const { betterAuth } = await import("better-auth");

//   const backendAuth = betterAuth({
//     database: new Pool({
//       connectionString: process.env.DATABASE_URL,
//     }),
//   });

//   return backendAuth;
// };

// const backendAuth = betterAuth({
//   database: new Pool({
//     connectionString: process.env.DATABASE_URL,
//   }),
// });

// export default backendAuth;
